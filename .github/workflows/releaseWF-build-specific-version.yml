name: Z-3 Build Custom Version

on:
  workflow_dispatch:

jobs:
  release-candidate:
    runs-on: ubuntu-latest
    steps:    
    - name: Wrong branch
      run: echo "WF is under development. Please run on GEMMA-release-master"
